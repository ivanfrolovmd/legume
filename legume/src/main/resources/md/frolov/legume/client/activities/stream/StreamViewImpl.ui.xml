<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
             xmlns:b="urn:import:com.github.gwtbootstrap.client.ui"
             xmlns:g="urn:import:com.google.gwt.user.client.ui">
    <ui:style>
        .scrollContainer {
            padding: 10px 20px;
        }

        .container {
        }

        .island {
            border: 1px solid #D8D8D8;
            border-bottom-width: 2px;
            background: white;
            border-radius: 3px;

            padding: 10px;
            margin: 10px 0;

            text-align: center;
            color: #333;
        }

        .loadingBar {
            margin: 10px 0;
        }
    </ui:style>
    <g:ScrollPanel ui:field="scrollContainer" styleName="{style.scrollContainer}">
        <g:FlowPanel>
            <g:FlowPanel ui:field="resultsPanel"> <!-- Panel with results -->
                <b:ProgressBar percent="100" type="ANIMATED" height="5px" addStyleNames="{style.loadingBar}" visible="false" ui:field="topLoading"/>
                <g:FlowPanel ui:field="topNoMoreResults" styleName="{style.island}" visible="false">
                    <b:Button ui:field="topTryAgain">More</b:Button>
                </g:FlowPanel>

                <g:FlowPanel ui:field="container" styleName="{style.container}"/>

                <b:ProgressBar percent="100" type="ANIMATED" height="5px" addStyleNames="{style.loadingBar}" visible="false" ui:field="bottomLoading"/>
                <g:FlowPanel ui:field="bottomNoMoreResults" styleName="{style.island}" visible="false">
                    <b:ButtonGroup>
                        <b:Button ui:field="bottomTryAgain">More</b:Button>
                        <b:Button ui:field="bottomAutoFetch" toggle="true">Auto fetch</b:Button>
                    </b:ButtonGroup>
                </g:FlowPanel>
            </g:FlowPanel>

            <g:FlowPanel ui:field="nothingFound" visible="false">
                <g:Label>Nothing found</g:Label>
            </g:FlowPanel>
        </g:FlowPanel>
    </g:ScrollPanel>
</ui:UiBinder>
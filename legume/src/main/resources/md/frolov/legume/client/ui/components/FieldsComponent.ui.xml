<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:b="urn:import:com.github.gwtbootstrap.client.ui"
             xmlns:g='urn:import:com.google.gwt.user.client.ui'>
    <ui:style>
        .box {
            padding: 10px;
        }

        .accordion {
        }

        @external .accordion-group;
        .accordion .accordion-group {
            border: none;
        }

        @external .accordion-heading;
        .accordion .accordion-heading {
            margin-left: 0;
            margin-right: 0;
        }

        .accordion a, .accordion a:hover {
            color: #333;
            text-decoration: none;
        }

        .accordion a i {
            margin-right: 3px; /* More space between text and icon */
        }

        @external .accordion-body;
        @external .in;
        .accordion .accordion-body.in, .accordion .accordion-body.in:hover  {
            overflow: visible;
        }
    </ui:style>
    <g:ScrollPanel styleName="{style.box}">
        <g:FlowPanel>
            <b:Accordion ui:field="typesAccordion" addStyleNames="{style.accordion}"/>
        </g:FlowPanel>
    </g:ScrollPanel>
</ui:UiBinder>
<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:b="urn:import:com.github.gwtbootstrap.client.ui"
             xmlns:g='urn:import:com.google.gwt.user.client.ui'>
    <!-- TODO fix loayout here -->
    <ui:style>
        .box {
            border-bottom: 1px solid #e5e5e5;
            position: relative;
        }

        .keyLabel {
            position: absolute;
            left: 0;
            width: 100px;
            top: 0;
            bottom: 0;
        }
        .controls {
            position: absolute;
            left: 105px;
            width: 52px;
            top: 0;
            bottom: 0;
            background-color: #f5f5f5;
        }

        .keyLabel {
            font-family: OpenSansLight;
            white-space: nowrap;
            overflow: hidden;
            color: #999;
            background-color: #f5f5f5;
        }

        .valueLabel {
            margin-left: 156px;
            white-space: pre;
        }

        .keyLabel, .valueLabel {
            padding: 2px 4px;
        }

        .cta {
            width: 2px;
        }
    </ui:style>
    <g:FlowPanel styleName="{style.box}" ui:field="container">
        <g:Label ui:field="keyLabel" styleName="{style.keyLabel}"/>
        <g:FlowPanel styleName="{style.controls}">
            <b:ButtonGroup>
                <b:Button size="MINI" type="LINK" icon="OK" ui:field="includeFilter" addStyleNames="{style.cta}"/>
                <b:Button size="MINI" type="LINK" icon="REMOVE" ui:field="excludeFilter" addStyleNames="{style.cta}"/>
                <b:Button size="MINI" type="LINK" icon="COPY" ui:field="copyToClipboard" addStyleNames="{style.cta}"/>
            </b:ButtonGroup>
        </g:FlowPanel>
        <g:Label ui:field="valueLabel" styleName="{style.valueLabel}"/>
    </g:FlowPanel>
</ui:UiBinder>